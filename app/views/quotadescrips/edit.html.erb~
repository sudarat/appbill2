<h1>Edit description</h1>

<% flash .each do |key, value| %>
  <% if value ==  "Description Error." %>
  <%= content_tag(:div, value, :class => "descrip") %>
  <% else %>
  <%= content_tag(:div, value, :class => "flash #{key}") %> 
  <% end %>
<% end %>

<%= form_for([@customer,@quotation, @quotadescrip]) do |qd|  %>
  <div class="field">
    <%= qd.label :description %><br /> 
    <%= qd.text_field :description %> <b><font color=red>&nbsp;*</font></b>
  </div>
  <div class="field">
    <%= qd.label :quantity %><br /> 
    <%= qd.text_field :quantity %> <b><font color=red>&nbsp;*</font></b>
  </div>
  <div class="field">
    <%= qd.label :unit_price %><br /> 
    <%= qd.text_field :unit_price %> <b><font color=red>&nbsp;*</font></b>
  </div>


  <div class="actions">
    <%= qd.submit %>
  </div>
<% end %>

<% cookies[:back] = request.referer %>
<%= link_to back, cookies[:back],:format => "html" %>